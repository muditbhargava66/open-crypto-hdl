# ============================================================
# .gitignore — open-crypto-hdl
# Cryptographic HDL cores targeting TinyTapeout (sky130) & FPGA
# ============================================================

# ── OS / Editor ──────────────────────────────────────────────
.DS_Store
Thumbs.db
*~
*.swp
*.swo
*.bak
.vscode/
.idea/

# ── Simulation Waveforms & Outputs ───────────────────────────
# VCD / FST / LXT / GHW dumps (large, regenerable from testbenches)
*.vcd
*.fst
*.lxt
*.lxt2
*.ghw

# Icarus Verilog compiled simulation binaries (root-level leftovers)
build_sim_*

# cocotb / Icarus simulation build directory
sim_build/
results.xml
__pycache__/
*.pyc

# ── Build Directory ──────────────────────────────────────────
# All Makefile-generated artifacts go here
build/

# ── Yosys Synthesis Artifacts ────────────────────────────────
*.blif
*.edif
*.json.synth
*_netlist.v

# ── OpenLane / ASIC Flow ─────────────────────────────────────
runs/
openlane_run/
*.gds
*.lef
*.mag
*.def
*.spef
*.sdf
*.lib

# ── SymbiYosys Formal Verification ───────────────────────────
chacha20_formal/
des_formal/
formal_results/
*_formal/

# ── Verilator Lint / Simulation ──────────────────────────────
obj_dir/

# ── Vivado / Xilinx ─────────────────────────────────────────
*.xpr
*.cache/
*.hw/
*.ip_user_files/
*.sim/
*.runs/
*.gen/
vivado_pid*
vivado*.log
vivado*.jou
*.bit
*.mcs
*.ltx

# ── Quartus / Intel FPGA ────────────────────────────────────
db/
incremental_db/
output_files/
*.sof
*.pof
*.jdi
*.pin
*.qpf
*.qdf
simulation/
*.sld

# ── Lattice ─────────────────────────────────────────────────
impl*/
*.dir/
.build_status

# ── ModelSim / QuestaSim ─────────────────────────────────────
*.wlf
*.vstf
*.ucdb
transcript
*.mti
modelsim.ini
work/

# ── Archives (regenerable) ──────────────────────────────────
*.tar.gz
*.zip
*.7z

# ── Python (cocotb, reference model, SPI driver) ────────────
__pycache__/
*.py[codz]
*$py.class
*.egg-info/
*.egg
.eggs/
dist/
*.so
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# ── Logs ─────────────────────────────────────────────────────
*.log
*.jou

# ── sv2v Conversion Output ───────────────────────────────────
compatible/

# ── FuseSoC Cache ────────────────────────────────────────────
.fusesoc/
fusesoc_libraries/